"""Runnable scripts, one per idea, each printing a table rather than asserting one.

The tests in this package prove things. These show them. Every script here is meant to be run
and read side by side with the module it draws on, which is why they print intermediate state
rather than a verdict: the verdict is in the tests already.
"""

from __future__ import annotations
